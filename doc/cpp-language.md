一句话能说清楚的，绝对不要用两句话

## C/C++语言 ##
1.什么是虚函数指针（vptr）？

答：

2.什么是虚函数表（vtbl）?

答：

3.虚函数的作用？

答：

4.深拷贝和浅拷贝的区别？

答：

5.怎么写赋值（“=”）运算符重载函数？

答：

6.sizeof一个空类结果是多少？为什么？

答：1个字节；否则无法初始化。

7.如果给空类添加一个构造函数和析构函数，sizeof是多少？为什么？

答：

8.接题目7，如果把析构设置成虚函数，sizeof的结果是多少？

答：32位是4，64位是8。

9.构造函数为什么不能为虚函数？

答：

10.哪些函数不能为虚函数？

答：

11.C++对象在内存怎么布局的？

答：

12.C++不能重载的运算符有哪些？

答：

13.菱形继承的二义性怎么解决？

答：

14.什么是纯虚函数？

答：

15.C++中显示转换有哪些？

答：

16.静态成员函数的属性是什么？

答：

17.静态成员需要在哪里定义？

答：

18.静态成员函数里能访问非静态成员吗？为什么？

答：不能，没有this指针。

19.指针和引用的区别？

答：

（1）引用必须被初始化，指针不必。

（2) 引用初始化以后不能被改变，指针可以改变所指的对象。

（3) 不存在指向空值的引用，但是存在指向空值的指针。

20.RAII机制是什么？

答：

21.C++11有哪些特性？

答：

22.智能指针有几种？怎么使用？

答：

23.C++11 move 语义？

答：

24.switch()的参数类型不能为什么类型？

答：实数型。

25.为什么gets越界崩溃？

答：碰到回车符才停止。建议使用fgets。

26.new/delete和malloc/free区别？

答：

27.overload、override、overwrite是什么？

答：overload是重载；override是覆盖；overwrite是重写。

28.怎么防止某个虚函数被重写？

答：加final。

29.class和struct区别？

答：

30.volatile关键字的作用？

答：

31.vector的size和capacity的区别？

答：

32.vector怎么使capacity为0？

答：

33.vector中resize和reserve的区别？

答：

34.C++是不是类型安全的？为什么？

答：不是，指针可以做强转换（reinterpret cast）。

35.main 函数执行以前，还会执行什么代码？是什么？

答：有，全局对象的构造函数。

36.浮点数和0的比较？

答：const EXPRESSION EXP = 0.000001

if ( a < EXP && a >-EXP)

37.const与#define 相比，有何优点？

答：

(1)被const修饰的东西都受到强制保护，可以预防意外的变动，能提高程序的健壮性;

(2)const 常量有数据类型，而宏常量没有数据类型。编译器可以进行类型安全检查

38.数组与指针的区别？

答： char a[] = "hello world";

char *p = a;

cout<< sizeof(a) << endl; // 12 字节

cout<< sizeof(p) << endl; // 4 字节

39.数组指针和指针数组的区别？

答：

40.什么是函数指针？

答：

41.STL仿函数和指针的差别？

答：

42.STL中的list底层数据结构是什么？

答：双链表。

43.STL中的map底层数据结构是什么？

答：红黑树。

44.map的key可以重复吗？

答：

45.STL中的unordered_map底层数据结构是什么？

答：哈希表。

46.C++有哪些性质？

答：封装、继承、多态。

47.对象作为参数和返回值有什么后果？

答：

48.虚函数的应用场景？

答：

49.C和C++内存分配有哪些区？

答：

50.sizeof和strlen的区别

答：

51.一个指针可以是volatile吗？

答：可以。

52.写一个“标准”宏MAX？

答：x,y不同类型的时候提示不能比较:

    #define MAX(x, y)  ({\
    typeof(x) _x = x;\
    typeof(y) _y = y;\
    (void)(&_x = &_y)\
    _x > _y ? _x : _y;\
    })

53.mencpy和memmove的区别？

答：

54.typedef和define有什么区别？

答：

55.extern有什么作用？

答：

56.指针常量与常量指针区别？

答：

57.写一个线程安全的单例模式？

答：

58.内联函数和宏定义的区别？

答：

59.exit() _exit()的区别？

答：

60.写一个c程序辨别系统是大端or小端字节序？

答：




    
